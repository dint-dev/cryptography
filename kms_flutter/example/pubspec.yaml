name: example
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  kms_flutter:
    path: ../

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test: ^1.0.2
  pedantic: ^1.11.0

dependency_overrides:
  crypto: ^3.0.0
  cryptography:
    path: '../../cryptography'
  kms:
    path: '../../kms'